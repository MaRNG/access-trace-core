Using PHP8.3
Using Nette
Using SQLite

Add declare(strict_types=1); to every php file
Add final to every final class
Add readonly to every class that not extends from other class
If class is not readonly, then add readonly to every constructor property, where it is possible